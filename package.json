{
  "name": "codex-web-demo",
  "version": "0.2.0",
  "private": true,
  "description": "Web client for Codex app-server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check script.js && node --check protocol.js && node --check transcript.js && node --check markdown.js && node --check claude-engine.js",
    "lint": "npm run check",
    "test": "node --test",
    "test:mobile": "node scripts/mobile-check.mjs"
  }
}
